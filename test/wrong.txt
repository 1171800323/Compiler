struct student{
    int id ;
    char name ;
}
    char [2][3] string;
    string[1][1] = 'a';
    string[0][2] = 'b';
    char ch;
    ch = string[1][1];

    int a;
    int b;
    int c;
    int d;
    b = 1 + 2;
    a = b * 2;
    c = 1e2 + a;
    d = 1;

    while (a < b)
    do if (c < d) then
          d = a + b;
       else
          d = a * b;

    float f;
    f = 10.0;
    f = a + f;

    /* 声明 */
    proc int getSum(int x, int y){
         int z;
         z = x + y;
         return z;
    }
    /* 调用 */
    call Sum(a, d);