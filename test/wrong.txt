struct student{
    int id ;
    char name ;
}
    char [2][3] string;
    string[0][1] = 'a';
    string[0][2] = 'b';
    char ch;
    ch = string[0][1];

    int a;
    int b;
    b = 1 + 2;
    a = b * 2;
    float c;
    c = 1e2 + a;
    int d;
    d = 2;
    float f;
    f = 10.0;

    int x;
    int y;
    int z;
    while (a < b)
    do if (c < d) then
          x = a + c;
       else
          x = a * c;

    /* 声明 */
    proc int getSum(int x, int y){
         int z;
         z = x + y;
         return z;
    }
    /* 调用 */
    call Sum(a, d);
