struct Node{
    float value;
    String element;
};
int main(){ 
    int a = 2;
    int b = 4;
    int c = a * b;
    bool flag = true;
    String string = "xyz";
    struct Node node;
    node.value = 1.2;
    node.element = "abc";
    
    while(c > 0 && a < c){
        print("Hello World!");
        c--;
        if(c / 2 == 2){
            break;
        }
    }
    if(a > b || b >= c){
        print(string);
    }else{
        print("node:value = " + node.value + ", element = " +node.element);
    }
}